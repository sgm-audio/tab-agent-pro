# Tab Agent MVP - Hugging Face Spaces Requirements
# Optimized for Zero GPU deployment with Basic Pitch
# Python 3.10+ (HF Spaces standard)

# Zero GPU support for HF Spaces
spaces

# Web Interface
gradio

# Core transcription (Basic Pitch with TensorFlow)
basic-pitch==0.3.2
tensorflow>=2.15.0,<2.16.0
numpy<2.0  # Required for TensorFlow compatibility

# Audio processing
demucs==4.0.1
librosa==0.10.2
soundfile==0.12.1
scipy>=1.11.0

# MIDI handling
note-seq==0.0.5
pretty-midi==0.2.10
mido==1.3.3

# PyTorch (CPU-only for fallback, Demucs)
torch>=2.0.0
torchaudio>=2.0.0
