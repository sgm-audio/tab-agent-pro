# Tab Agent MVP - Hugging Face Spaces Requirements
# Optimized for CPU-basic deployment

# Core transcription (Basic Pitch with ONNX backend, no TF needed)
basic-pitch==0.3.2

# Audio processing
librosa==0.10.2
soundfile==0.12.1
scipy>=1.11.0
numpy

# MIDI handling
pretty-midi==0.2.10

# No TensorFlow - Basic Pitch falls back to ONNX runtime on CPU
# No Demucs - too heavy for free CPU tier (add with ZeroGPU later)
# No PyTorch - not needed for Basic Pitch transcription
