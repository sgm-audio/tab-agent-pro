# Tab Agent MVP - Docker Space Requirements
gradio>=4.0.0

# Zero GPU support (HuggingFace Spaces)
spaces>=0.19.0

# Core transcription (Basic Pitch with TensorFlow)
# Note: tensorflow<2.16.0 is the last version supporting Python 3.10
basic-pitch==0.3.2
tensorflow>=2.4.1,<2.16.0
numpy<2.0

# YourMT3+ transformer model support
transformers>=4.48.0
huggingface_hub>=0.20.0

# Demucs stem separation (Python API, no CLI dependency)
demucs>=4.0.0

# Audio processing
librosa==0.10.2
soundfile==0.12.1
scipy>=1.11.0

# MIDI handling
pretty-midi==0.2.10
note-seq==0.0.5
IPython>=8.0,<8.30

# Gradio is handled by HF Spaces runtime
